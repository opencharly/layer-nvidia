# 2026.245.1521 — fix(ci): adopt the org-standard workflow_run-based tag-on-merge dispatcher

## Summary

This repo's tag-on-merge dispatcher (old `pull_request: [closed]` design, org reusable pinned at `@v1.0.0`) has produced ZERO tag-on-merge runs since provisioning — verified API-side (`workflows/tag-on-merge.yml/runs?total_count=0`). The org file header documents the measured RCA: **native auto-merge squash merges fire their webhook events but GitHub Actions creates zero workflow runs for them** (platform-level run suppression), so `pull_request: [closed]` / `push` cannot observe them; the proven trigger is `workflow_run` on `charly/pr-validator` (whose final step arms the auto-merge) + a bounded wait-for-merge probe. Install the org-standard dispatcher **byte-identical** to distro-omarchy / the org reference (verified with `diff`).

## How tested

- The file is byte-identical to distro-omarchy's dispatcher (which tags reliably): `diff <(org file) .github/workflows/tag-on-merge.yml` → empty.
- The verifier is the next merge event: the org reusable runs on the `charly/pr-validator` completion, waits for the merge, and tags it with the CalVer + CHANGELOG; the org reusable's idle sweep also self-heals the repo's earlier un-tagged merges.

## Rulebook compliance

- **R0/R1:** tag absence root-caused (RCA documented in the org file header — measured across the org repo's 8 merges/17 schedule runs); the correct reference implementation is the org's own file.
- **R2:** CI-file only; org-parity install (R3 — the org dispatcher is THE canonical instance; no hand-authored variant).
- **R4:** no workaround — this is the org's proven trigger design.
- **R5:** hard cutover — the old dispatcher file is replaced by the org standard in this same PR.
- **R6:** feature branch off origin/main; no force-push.
- **R7:** the gate is tag emission on the next merge (org reusable invocation).
- **R10:** CI-config; tier analysed on a live system (the repo's zero-run history is the negative baseline; the sibling repo's working dispatcher is the reference).

## Change classification

- **Change class:** CI fix — tag-on-merge dispatcher migration to the org standard
- **Verification gate:** post-merge tag emission measured (org reusable run)
- **Attribution tier:** analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*