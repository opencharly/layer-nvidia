# 2026.245.1515 — fix(ci): point the tag-on-merge dispatcher at the org reusable @main

## Summary

This repo's `tag-on-merge.yml` dispatcher referenced the org reusable at `@v1.0.0`, a ref that does not exist — so the repo has emitted ZERO tag-on-merge runs since provisioning (verified: `runs?total_count=0` while sibling repos tag correctly). Sibling repos (distro-omarchy, layer-wf-recorder) pin `@main`. Point the dispatcher at `@main`.

## How tested

- Diff: one line (`@v1.0.0` → `@main`).
- The proof of the fix is the event itself: after this PR merges, the `pull_request: [closed]` event fires the dispatcher, which calls the org reusable and creates the CalVer tag + CHANGELOG — that tag's existence on this merge is the verifier (the org reusable's sweep also self-heals the two earlier un-tagged merges of this repo).

## Rulebook compliance

- **R0/R1:** tag absence root-caused to the scaffolded `@v1.0.0` ref (nonexistent); sibling-repo comparison confirms `@main` is the working ref.
- **R2:** single-line CI fix; nothing else touched.
- **R3/R4:** aligns with the org dispatcher standard (R3), no workarounds.
- **R5:** N/A — CI file.
- **R6:** feature branch off origin/main; no force-push.
- **R7:** the gate is the org reusable invocation on the merge event (workflow runs list).
- **R10:** config/CI-only change; tier analysed on a live system (the repo's own run history is the baseline).

## Change classification

- **Change class:** CI fix — workflow reusable ref
- **Verification gate:** post-merge tag emission (org reusable run on the closed event)
- **Attribution tier:** analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*