# 2026.245.1456 — fix(ci): keep the schema stamp at the CI-pinned charly floor

## Summary

#2 landed the omarchy distro arm + gpgcheck scoping but also migrated the schema stamp (2026.232.0520 → 2026.240.1943), which is NEWER than the charly the repo's candy gate pins (schema max 2026.232.0520) — the `candy` gate broke on the merged head:

```
charly box validate
charly: error: command "validate": validation error: charly.yml: config schema 2026.240.1943 is newer than this charly supports (max 2026.232.0520)
```

This PR reverts ONLY the schema stamp. The omarchy distro arm and the `exclude_distro: [omarchy]` scoping from #2 are authored entirely within the 2026.232-compatible surface and stay (the pinned CI charly validates them).

## How tested

- `charly.yml` diff: single line (`version: 2026.240.1943` → `version: 2026.232.0520`); omarchy arm + exclude entries present (grep: 2 omarchy references).
- The pinned CI gate is the verifier for the exact schema ceiling: the `candy` workflow runs `.ci/charly/bin/charly box validate` — it must pass with the reverted stamp.
- Local validation with the current charly (newer) still passes the structural changes (validated in #2 with 0 warnings/errors).

## Rulebook compliance

- **R0/R1:** CI failure surfaced after #2 merged — root-caused to the schema-stamp migration vs the repo's pinned CI charly; fix reverts the unnecessary stamp.
- **R2:** single-line revert; the feature additions of #2 are untouched.
- **R3/R4:** no new mechanism; the revert restores the authored schema floor.
- **R5:** N/A — additive config, nothing removed beyond the bump.
- **R6:** feature branch off origin/main; no force-push.
- **R7:** the pinned CI gate is the gate; it reruns on this PR.
- **R10:** config-only single-line change; the gate run is the proof. Tier: analysed on a live system (the #2 bed-run evidence remains the feature proof).

## Change classification

- **Change class:** candy/box-config — schema stamp revert (CI compatibility)
- **Verification gate:** `candy` CI validate on this PR (pinned charly schema ceiling)
- **Attribution tier:** analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*